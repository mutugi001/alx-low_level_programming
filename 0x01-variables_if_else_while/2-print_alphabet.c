#include <stdio.h>
/**
 * main-Entry point
 * Description:'small alpha letters'
 * Return:Always 0 (success)
 */
int main(void);
{
	char ch = 'a';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	return (0);



}
