#include "main.h"
/**
 * print_most_numbers- prints nums except 1 and 4
 * Return:Always 0
 */

void print_most_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		if (a != '4' && a != '2')
		{
			_putchar(num);
		}
		else
			continue;

	}

		_putchar('\n');
}
