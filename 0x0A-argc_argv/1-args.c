#include "main.h"
#include <stdio.h>
/**
 * main-prints number of arg
 * @argc: arg count
 * @argv: arg
 * Return:Always 0
 */
int main(int argc, char *argv[])
{
	void (argv);
	printf("%i\n", argc - 1);
	return (0);
}
